{
  "name": "agenda-aulas-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Testes end-to-end (Cypress) do sistema Agenda Aulas — cobre cadastro, login, criação de horários e o fluxo completo de reserva.",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headed": "cypress run --headed"
  },
  "devDependencies": {
    "cypress": "^13.15.0"
  }
}
